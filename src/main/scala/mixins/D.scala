package mixins

class D extends B with C

// class D has a superclass B and a mixin C
// classes can have only one super class but any number of mixins
// mixins and the superclass can have same supertype
